it2234
Web Services and Server Technology


1.
This project demonstrates a simple **Express.js** server that listens on **port 3001** and responds with `"Hello Express JS"` when accessed.

2
This project demonstrates a basic **Node.js HTTP server** using the built-in `http` module. The server listens on **port 3000** and responds with `"Hello Node JS"`.
